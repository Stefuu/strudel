{
  "buildCommand": "pnpm install --yes && cd website && pnpm install --yes && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --yes",
  "framework": "astro",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 