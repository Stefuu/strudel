{
  "buildCommand": "pnpm install --unsafe-perm && cd website && pnpm install --unsafe-perm && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --unsafe-perm",
  "framework": "astro",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 