{
  "buildCommand": "pnpm install && pnpm approve-builds && cd website && pnpm install && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "astro",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 